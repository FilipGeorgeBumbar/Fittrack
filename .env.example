# Client (Vite) — for development
VITE_SERVER_IP=localhost
VITE_SERVER_PORT=3000
VITE_USE_HTTPS=true
# Inactivity logout (milliseconds), default 30 minutes
VITE_SESSION_IDLE_MS=1800000

# Production only — set this to your Render backend URL
# VITE_API_URL=https://your-app-api.onrender.com
